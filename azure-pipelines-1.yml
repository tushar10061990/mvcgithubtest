pool: MyComputerAagent

variables: 
  - group: vargroup
  - name: os
    value: window

steps:
  - script: | 
     echo $(os)
     echo $(inputvar)
     echo variable group $(var1)
     echo "##vso[task setvariable variable=os]mac"
     echo $(os)
    displayName: step2
